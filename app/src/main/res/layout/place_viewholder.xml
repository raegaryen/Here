<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/placeViewholderContainer"
    android:orientation="vertical"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <ImageView
        android:id="@+id/placeViewholderImage"
        android:layout_width="@dimen/image_size"
        android:layout_height="@dimen/image_size"
        android:src="@drawable/cat_06"/>

    <TextView
        android:id="@+id/placeViewholderVicinity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/placeViewholderImage"
        tools:text="Invalidenstraße 116, 10115 Berlin"/>

    <TextView
        android:id="@+id/placeViewholderDistance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/placeViewholderVicinity"
        android:textColor="@android:color/black"
        tools:text="10 m"/>

    <TextView
        android:id="@+id/placeViewholderTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textColor="@android:color/black"
        android:textStyle="bold"
        android:layout_gravity="center_vertical"
        tools:text="Here Berlin"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@id/placeViewholderImage"
        android:layout_above="@id/placeViewholderVicinity" />

</RelativeLayout>